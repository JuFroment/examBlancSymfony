{% extends 'base.html.twig' %}

{% block title %}Employé{% endblock %}

{% block body %}
    <div class="text-center my-5">
    <h1>Fiche de {{ user.prenom }} {{ user.nom }}</h1>
    </div>
<div class="container">
    <div class="row justify-content-center">
    <div class="card" style="width: 24rem;">
        <img src="{{ user.photo }}" class="card-img-top" alt="...">
        <div class="card-body">
            <h5 class="card-title"><strong>{{ user.nom }} {{ user.prenom }}</strong></h5>
            <h5 class="card-title"><strong>email : </strong>{{ user.email }}</h5>
            <h5 class="card-title"><strong>Secteur : </strong>{{ user.secteur }}</h5>
        </div>
    </div>
</div>
</div>
    <div class="text-center">

    <a href="{{ path('app_user_crud_index') }}"><i class="fa-solid text-black fa-xl m-3 fa-arrow-left"></i></a>

    <a href="{{ path('app_user_crud_edit', {'id': user.id}) }}"><i class="fas text-black fa-xl m-3 fa-edit"></i></a>

    {{ include('user_crud/_delete_form.html.twig') }}
    </div>
{% endblock %}
